import { CATALOG, eligible } from './catalog.mjs';
const esc = value => String(value ?? '').replace(/[&<>"']/g, x => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));
export function page(title, body, active = '') {
 const nav=[['/','Overview'],['/connect','Connect'],['/projects','Projects'],['/automations','Automations'],['/rules','Rules'],['/reference','Functions & endpoints'],['/status','Status']];
 return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · Bittrees MCP</title><meta name="description" content="Connect agents to Bittrees projects. Configure scoped integrations, rules and automation."><link rel="stylesheet" href="/assets/site.css"></head><body><a class="skip" href="#main">Skip to content</a><header><a class="brand" href="/"><span class="mark">b.</span> Bittrees <span class="muted">MCP</span></a><nav aria-label="Main navigation">${nav.map(([href,label])=>`<a href="${href}" ${active===href?'aria-current="page"':''}>${label}</a>`).join('')}</nav><a class="onboarding" href="https://agent.bittrees.org">Agent onboarding ↗</a></header><main id="main">${body}</main><footer><span>Bittrees MCP · Independent integration service</span><a href="https://github.com/Bittrees-Technology/mcp">Source & documentation ↗</a><a href="/reference">Functions & endpoints</a><a href="/mcp-docs">Client guide</a><a href="/health">Service health</a></footer></body></html>`;
}
export function home(catalog=CATALOG){
const count=catalog.projects.filter(eligible).length;
return page('Connect your ecosystem',`<section class="hero"><p class="eyebrow">ONE CONNECTION. YOUR PROJECTS.</p><h1>The connection layer<br>for your agents.</h1><p class="lead">Bring Bittrees projects into your agent’s workspace. Choose your scope, set the rules, and keep every automation accountable.</p><div class="actions"><a class="button" href="/connect">Connect your agent <span>↗</span></a><a class="secondary" href="/projects">Explore projects →</a></div><div class="endpoint"><span>STREAMABLE HTTP</span><code>https://mcp.bittrees.org/mcp</code></div></section><section class="stats" aria-label="Service capabilities"><div><strong>${count}</strong><span>approved projects</span></div><div><strong>3</strong><span>connection choices</span></div><div><strong>Scoped</strong><span>rules & permissions</span></div><div><strong>Recorded</strong><span>automation history</span></div></section><section class="section"><div class="section-heading"><div><p class="eyebrow">A CLEAR PATH FROM CONNECTION TO EXECUTION</p><h2>Start small. Connect more.</h2></div><a href="/mcp-docs">Read the setup guide →</a></div><div class="grid"><article class="card"><span class="number">01 / CONNECT</span><h3>Your projects, your scope</h3><p>Select individual projects, all Bittrees projects, or the wider ecosystem. Your connection carries the selection with it.</p><a href="/connect">Choose projects →</a></article><article class="card"><span class="number">02 / SET RULES</span><h3>Make permission explicit</h3><p>Versioned rules specify which projects and tools an automation may use. Current permissions are checked on every attempt.</p><a href="/rules">Manage rules →</a></article><article class="card"><span class="number">03 / AUTOMATE</span><h3>Keep work observable</h3><p>Create paused automations, enable them when ready, and inspect their runs. Manual, scheduled and event triggers share an audit trail.</p><a href="/automations">Open automations →</a></article></div></section><section class="callout"><div><p class="eyebrow">AVAILABLE TODAY</p><h2>Public project context.<br>A foundation you can inspect.</h2></div><div><p>Discover approved projects, read their context, and prepare handoffs. Automations can capture that public context in durable history.</p><p class="muted">Private product APIs, messages and transactions remain separate integrations. Project inclusion does not grant access.</p><a href="/status">See integration status →</a></div></section>`, '/');}
export function projects(catalog=CATALOG){return page('Projects',`<section class="intro"><p class="eyebrow">THE BITTREES ECOSYSTEM</p><h1>Every project.<br>A clear connection.</h1><p class="lead">Explore available context and planned integrations. Only approved projects are included in connection profiles.</p><a class="button" href="/connect">Choose your connection →</a></section><div class="grid projects">${catalog.projects.map(p=>`<article class="card"><span class="pill">${esc(p.affiliation)} · ${esc(p.approval)}</span><h2>${esc(p.name)}</h2><p>${esc(p.summary)}</p><p class="muted">${eligible(p)?'Public context available':'Pending review'} · Product actions not connected</p>${eligible(p)?`<a href="/connect?mode=selected&projects=${encodeURIComponent(p.id)}">Connect this project →</a>`:''}${p.publicUrl?`<a class="project-site" href="${esc(p.publicUrl)}">Visit project ↗</a>`:''}</article>`).join('')}</div>`, '/projects');}
export function workspace(kind){const rules=kind==='rules';return page(rules?'Rules':'Automations',`<div class="work-shell" data-workspace-page="${kind}"><section class="work-heading"><div><h1 id="workspace-title">${rules?'Rules':'Automations'}</h1><p id="workspace-description">${rules?'Decide which projects an automation can read. Turn permissions on or off without editing code.':'Save a project update on a schedule, or run it when you need it.'}</p></div><a id="switch-view" href="${rules?'/automations':'/rules'}">${rules?'View automations':'Manage rules'}</a></section>
<section class="access-panel"><form id="login"><label for="token">Open your workspace</label><p class="muted">Enter the access key supplied by your Bittrees MCP operator. You do not need a key to <a href="/projects">browse public projects</a>.</p><div class="login-row"><input id="token" type="password" autocomplete="off" required placeholder="Workspace access key" aria-label="Workspace access key"><button>Open workspace</button></div></form><div id="connected" hidden><strong>Workspace connected</strong><span>Your key stays in this tab only.</span><button type="button" id="disconnect" class="secondary">Disconnect</button></div><p id="notice" role="status" aria-live="polite">Automations can read public project information. They cannot send messages, move funds or change project permissions.</p></section>
<div id="workspace" hidden><div class="workspace-toolbar"><p id="access-summary"></p><button id="refresh" class="secondary">Refresh activity</button></div>
<section id="automation-view"><section id="ai-connections-panel" class="work-list" hidden><h2>Your AI connections</h2><p>Approve one local template in AI, then choose it when creating an automation.</p><button id="ai-connect" type="button">Connect an AI template</button><div id="ai-consent" hidden class="work-row"><p>Open <a href="https://ai.bittrees.org" target="_blank" rel="noopener noreferrer">Bittrees AI</a> and enter this request ID and approval code in Connections.</p><label for="ai-request-id">Request ID</label><input id="ai-request-id" readonly autocomplete="off"><label for="ai-approval-code">Approval code</label><input id="ai-approval-code" readonly autocomplete="off"><p>After approving there, confirm the AI owner ID shown on that page.</p><form id="ai-confirm-form"><label for="ai-owner">AI owner ID</label><input id="ai-owner" required autocomplete="off"><label><input id="ai-confirmed" type="checkbox" required> I checked the owner and approved template in AI.</label><button type="submit">Finish connection</button></form></div><div id="ai-connections"></div></section><div class="work-builder"><form id="automation-form" class="work-form"><h2>Create an automation</h2><label for="automation-project">Project</label><select id="automation-project" name="projectId" required></select><label for="automation-connection">Action</label><select id="automation-connection" name="connectionId"><option value="">Save public project information</option></select><label for="automation-name">Name</label><input id="automation-name" name="name" maxlength="100" required placeholder="For example, daily research update"><label for="automation-timing">When should it run?</label><select id="automation-timing" name="timing"><option value="manual">Only when I run it</option><option value="3600">Every hour</option><option value="21600">Every six hours</option><option value="86400">Every day</option></select><p id="setup-access" class="muted"></p><button id="save-automation">Save paused automation</button></form><aside class="work-summary" aria-live="polite"><h2>What will happen</h2><p id="automation-preview">Choose a project to preview your automation.</p><ul id="automation-outcomes"><li>Reads the project’s public name, summary and catalog revision.</li><li>Saves the result in your activity history.</li><li>Creates a matching project selection and permission rule for you.</li></ul><p><strong>Nothing runs until you enable it.</strong></p><p class="muted">Scheduled work is processed about every five minutes. It may start later if the worker is busy.</p></aside></div><section class="work-list"><h2>Your automations</h2><div id="automations"></div></section></section>
<section id="rules-view" hidden><div class="work-builder"><form id="rule-form" class="work-form"><h2>Create a rule</h2><label for="rule-name">Name</label><input id="rule-name" name="name" maxlength="100" required placeholder="For example, research read access"><fieldset><legend>Allow these projects</legend><div id="rule-projects" class="project-checks"></div></fieldset><p>Allowed action: <strong>Read public project information</strong>.</p><p class="muted">A rule cannot give more access than your workspace key allows. Creating a rule does not start an automation.</p><button id="save-rule">Create rule</button></form><aside class="work-summary"><h2>How rules work</h2><p>Every automation checks its rule before each run. Disable a rule to prevent the next run from reading project information.</p><p>Automations created on this site already have a matching rule. You can manage those below.</p><p class="muted">Disabling a rule does not interrupt a run that has already completed its permission check. Changes are kept in version history.</p></aside></div><section class="work-list"><h2>Your rules</h2><div id="rules"></div></section></section>
<section class="work-list"><h2>Recent activity</h2><div id="activity"></div></section><details class="technical-details"><summary>Technical details</summary><p>For advanced integration and troubleshooting. <a href="/reference">Functions and endpoints</a></p><pre id="history"></pre></details></div>
<dialog id="confirm-dialog"><form method="dialog"><h2>Cancel this automation?</h2><p id="confirm-description"></p><p>Queued runs will be cancelled. This automation cannot be restarted. To stop temporarily, use Pause instead.</p><div class="work-actions"><button value="back" class="secondary">Keep automation</button><button value="cancel">Cancel automation</button></div></form></dialog></div><script src="/assets/workspace.js" defer></script>`, '/'+kind);}
