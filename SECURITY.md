# Security and release ownership

Bittrees-Technology owns this service and its independent releases. Report suspected vulnerabilities through GitHub private vulnerability reporting; do not include credentials or private tenant content in public issues. Catalog source controllers and pending accountable contacts are recorded per product.

Production deployment requires separate service credentials, PostgreSQL authorization and successful negative authorization checks. Catalog membership never grants a role or authority to execute downstream actions. Release automation does not activate product automations.
